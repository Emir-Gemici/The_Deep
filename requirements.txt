# =============================================================================
# The Deep — TEKNOFEST 2026 Yapay Zeka Dil Ajanlari Yarismasi (3. Senaryo)
# Bagimliliklar — dogrulanmis surumler (Python 3.13, Windows 11, RTX 5080 16GB)
#
# KURULUM SIRASI:
#   1) python -m venv .venv && .venv\Scripts\activate
#   2) NVIDIA GPU icin torch'u CUDA index'inden kurun (CPU-only torch YOLO
#      egitimini/cikarimini cok yavaslatir):
#      pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#   3) pip install -r requirements.txt
#   4) VLM katmani (Qwen2.5-VL) icin ayrica asagidaki "VLM" bolumunu kurun.
# =============================================================================

# --- Cekirdek: Tespit + Takip + Olay-turetme (tracking.py, olaylar.py, demo.py) ---
ultralytics==8.4.72
opencv-python==4.13.0.92
numpy==2.4.6
pillow==12.2.0
lap==0.5.13            # BoT-SORT eslestirme (tracking)
PyYAML==6.0.3

# torch / torchvision: yukaridaki 2. adimdaki komutla (CUDA 12.8) kurulmasi onerilir.
# GPU yoksa duz "pip install torch torchvision" da calisir (yavas).
torch==2.11.0
torchvision==0.26.0

# --- Arayuz: Streamlit canli izleme paneli (arayuz.py) ---
streamlit==1.58.0

# --- VLM katmani (vLLM.py — Qwen2.5-VL-7B-Instruct, 8-bit yerel) ---
# Bu bolum yalnizca VLM analiz katmanini calistiracak makinede gereklidir
# (>= 12GB VRAM onerilir). Kurulum:
#   pip install "transformers>=4.49" accelerate bitsandbytes qwen-vl-utils
# transformers>=4.49          # Qwen2.5-VL mimari destegi
# accelerate                  # device_map ile model yerlesimi
# bitsandbytes                # 8-bit nicemleme (load_in_8bit)
# qwen-vl-utils               # process_vision_info (gorsel on-isleme)
